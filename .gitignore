# Cached map tiles. Large (~150 MB for PWS z8-13), binary, and derived:
# regenerate with tools/fetch_tiles.py rather than committing them.
tiles/

# Exported survey data — the CSV/JSON exports are field data, not source.
survey_log_*.csv
survey_export_*.json

# OS / editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/
